const createReportEntity = ({
  reportId,
  sequence,
  cityCode,
  userId,
  title,
  category,
  description,
  latitude,
  longitude,
  location = null,
  exactZone = null,
  imageUrl = null,
  imageKeys = [],
  status,
  priority,
  createdAt,
  updatedAt,
  resolvedAt = null,
  upvotes = [],
  downvotes = [],
}) => ({
  reportId,
  sequence,
  cityCode,
  userId,
  title,
  category,
  description,
  latitude,
  longitude,
  location,
  exactZone,
  imageUrl,
  imageKeys,
  status,
  priority,
  createdAt,
  updatedAt,
  resolvedAt,
  upvotes,
  downvotes,
});

module.exports = {
  createReportEntity,
};
