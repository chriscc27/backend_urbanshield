const createReportEntity = ({
  reportId,
  userId,
  title,
  category,
  description,
  latitude,
  longitude,
  location = null,
  imageUrl = null,
  imageKeys = [],
  status,
  priority,
  createdAt,
  updatedAt,
  resolvedAt = null,
  upvotes = [],
  downvotes = [],
}) => ({
  reportId,
  userId,
  title,
  category,
  description,
  latitude,
  longitude,
  location,
  imageUrl,
  imageKeys,
  status,
  priority,
  createdAt,
  updatedAt,
  resolvedAt,
  upvotes,
  downvotes,
});

module.exports = {
  createReportEntity,
};
